import 'package:flutter/material.dart';

/// Creates the shared visual theme used by every architecture example.
ThemeData buildExampleTheme() => ThemeData(
  colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xff6857d9)),
  useMaterial3: true,
);
